Remove notification hooks from the current project's `.claude/` directory.

Run the removal script:

```bash
bash ~/.claude/plugins/marketplaces/cc-plugins/notifications/scripts/remove-notifications.sh
```
